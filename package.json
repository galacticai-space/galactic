{
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && node server.js"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "http": "^0.0.1-security",
    "path": "^0.12.7",
    "socket.io": "^4.8.1",
    "ws": "^8.18.0"
  }
}
